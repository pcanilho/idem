---
name: Bug report
about: Something idem got wrong
labels: bug
---

**What happened, and what you expected instead.**

**A chart that reproduces it.** This is by far the most useful thing you can include — if you can
reduce it to something the size of `examples/churning-chart`, paste it here.

```yaml
```

**The command you ran, and its output.**

```console
$ idem ...
```

**Versions**

- `idem --version`:
- `helm version --short`:
- which engine you run (ArgoCD / Flux / plain Helm), and its version:
